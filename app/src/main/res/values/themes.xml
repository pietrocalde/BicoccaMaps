<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BicoccaMaps" parent="Theme.Material3.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/unimib_red</item>
        <item name="colorPrimaryVariant">@color/unimib_red</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>

        <!-- Customize your theme here. -->

        <item name="android:textColorPrimary">@color/white</item>
        <item name="bottomNavigationStyle">@style/Widget.App.BottomNavigationView</item>
        <item name="borderlessButtonStyle">@style/Widget.App.Button.TextButton</item>
        <item name="textInputStyle">@style/Widget.App.TextInputLayout</item>
        <item name="searchViewStyle">@style/Widget.AppCompat.SearchView.ColorText</item>

        <!--COLORI CARD-->
        <item name="colorSurface">@color/unimib_red</item>
        <item name="colorOnSurfaceVariant">@color/white</item>
        <item name="shapeAppearanceMediumComponent">@style/ShapeAppearance.App.MediumComponent</item>
    </style>

    <!-- Style for NavigationView (NavigationDrawer) -->
    <style name="Widget.App.NavigationView" parent="Theme.BicoccaMaps">
        <!-- For NavigationView (NavigationDrawer) -->
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <!-- Style for TextInputLayout -->
    <style name="Widget.App.TextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="hintTextColor">@color/white</item>
    </style>

    <!-- Style for BottomNavigationView -->
    <style name="Widget.App.BottomNavigationView" parent="Widget.Material3.BottomNavigationView">

        <item name="backgroundTint">@color/unimib_red</item>
        <item name="android:onClick">@color/white</item>
    </style>

    <!-- Style for Button -->
    <style name="Widget.App.Button" parent="Widget.Material3.Button.ElevatedButton">
        <item name="android:backgroundTint">@color/unimib_red</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- Style for Material TextButton-->
    <style name="Widget.App.Button.TextButton" parent="Widget.Material3.Button.TextButton">
        <item name="android:textColor">@color/white</item>
        <item name="rippleColor">@color/white</item>
    </style>

    <!-- Style for Toolbar -->
    <style name="Widget.App.Toolbar" parent="Widget.Material3.Toolbar">
        <item name="android:background">@color/unimib_red</item>
        <item name="titleTextColor">@color/white</item>
    </style>

    <!-- Style for Card -->
    <style name="ShapeAppearance.App.MediumComponent" parent="ShapeAppearance.Material3.MediumComponent">
        <item name="cornerSize">8dp</item>
    </style>

    <style name="Widget.AppCompat.SearchView.ColorText" parent="Widget.AppCompat.SearchView">
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorHint">@android:color/white</item>
    </style>


</resources>